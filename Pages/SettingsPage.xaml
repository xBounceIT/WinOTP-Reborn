<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinOTP.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer>
        <StackPanel Padding="16" Spacing="8">
            <!-- Security Settings -->
            <Border
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8"
                Padding="12">
                <StackPanel Spacing="8">
                    <TextBlock
                        Text="Security"
                        Style="{StaticResource BodyStrongTextBlockStyle}" />
                    <ToggleSwitch
                        x:Name="PinProtectionToggle"
                        Header="Protect app with PIN"
                        Toggled="PinProtectionToggle_Toggled" />
                    <ToggleSwitch
                        x:Name="PasswordProtectionToggle"
                        Header="Protect app with password"
                        Toggled="PasswordProtectionToggle_Toggled" />
                    <ToggleSwitch
                        x:Name="WindowsHelloToggle"
                        Header="Protect app with Windows Hello"
                        Toggled="WindowsHelloToggle_Toggled" />
                    <StackPanel
                        x:Name="WindowsHelloRemoteFallbackPanel"
                        Margin="24,0,0,0"
                        Spacing="8"
                        Visibility="Collapsed">
                        <TextBlock
                            Text="Remote Desktop fallback"
                            Style="{StaticResource BodyStrongTextBlockStyle}" />
                        <TextBlock
                            Text="Windows Hello is unavailable over Remote Desktop. Add an optional PIN or password that WinOTP will require only while you are connected remotely."
                            Style="{StaticResource CaptionTextBlockStyle}"
                            Opacity="0.7"
                            TextWrapping="WrapWholeWords" />
                        <ToggleSwitch
                            x:Name="WindowsHelloRemotePinToggle"
                            Header="Require PIN over Remote Desktop"
                            Toggled="WindowsHelloRemotePinToggle_Toggled" />
                        <ToggleSwitch
                            x:Name="WindowsHelloRemotePasswordToggle"
                            Header="Require password over Remote Desktop"
                            Toggled="WindowsHelloRemotePasswordToggle_Toggled" />
                    </StackPanel>

                    <ComboBox
                        x:Name="AutoLockComboBox"
                        Header="Auto-lock after inactivity"
                        Width="200"
                        HorizontalAlignment="Left"
                        SelectionChanged="AutoLockComboBox_SelectionChanged">
                        <ComboBoxItem Content="Never" Tag="0" />
                        <ComboBoxItem Content="1 minute" Tag="1" />
                        <ComboBoxItem Content="2 minutes" Tag="2" />
                        <ComboBoxItem Content="5 minutes" Tag="5" />
                        <ComboBoxItem Content="10 minutes" Tag="10" />
                        <ComboBoxItem Content="15 minutes" Tag="15" />
                        <ComboBoxItem Content="30 minutes" Tag="30" />
                    </ComboBox>
                    <TextBlock
                        Text="Automatically lock the app after a period of inactivity when protection is enabled."
                        Style="{StaticResource CaptionTextBlockStyle}"
                        Opacity="0.7"
                        TextWrapping="WrapWholeWords" />
                </StackPanel>
            </Border>

            <!-- Display Settings -->
            <Border
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8"
                Padding="12">
                <StackPanel Spacing="8">
                    <TextBlock
                        Text="Display"
                        Style="{StaticResource BodyStrongTextBlockStyle}" />
                    <ToggleSwitch
                        x:Name="ShowNextCodeToggle"
                        Header="Show next TOTP code when 5 seconds remain"
                        Toggled="ShowNextCodeToggle_Toggled" />
                    <TextBlock
                        Text="Keeps the current code visible and shows the upcoming code as a hint in the last 5 seconds."
                        Style="{StaticResource CaptionTextBlockStyle}"
                        Opacity="0.7"
                        TextWrapping="WrapWholeWords" />
                </StackPanel>
            </Border>

            <!-- Backup Settings -->
            <Border
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8"
                Padding="12">
                <StackPanel Spacing="10">
                    <TextBlock
                        Text="Backup"
                        Style="{StaticResource BodyStrongTextBlockStyle}" />
                    <ToggleSwitch
                        x:Name="AutomaticBackupToggle"
                        Header="Enable automatic backup"
                        Toggled="AutomaticBackupToggle_Toggled" />
                    <TextBlock
                        Text="Password-protected backups are stored locally whenever your tokens change."
                        Style="{StaticResource CaptionTextBlockStyle}"
                        Opacity="0.7"
                        TextWrapping="WrapWholeWords" />
                    <TextBlock
                        x:Name="BackupFolderPathTextBlock"
                        Style="{StaticResource CaptionTextBlockStyle}"
                        Opacity="0.7"
                        TextWrapping="WrapWholeWords" />
                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Button
                            Grid.Column="0"
                            x:Name="BrowseBackupFolderButton"
                            Click="BrowseBackupFolderButton_Click"
                            HorizontalAlignment="Stretch"
                            Content="Browse" />
                        <Button
                            Grid.Column="1"
                            x:Name="ResetBackupFolderButton"
                            Click="ResetBackupFolderButton_Click"
                            HorizontalAlignment="Stretch"
                            Content="Reset to default" />
                    </Grid>
                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Button
                            Grid.Column="0"
                            x:Name="ImportBackupButton"
                            Click="ImportBackupButton_Click"
                            HorizontalAlignment="Stretch"
                            Content="Import backup" />
                        <Button
                            Grid.Column="1"
                            x:Name="ExportBackupButton"
                            Click="ExportBackupButton_Click"
                            HorizontalAlignment="Stretch"
                            Content="Export backup" />
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Update Settings -->
            <Border
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8"
                Padding="12">
                <StackPanel Spacing="10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel
                            Grid.Column="0"
                            Orientation="Horizontal"
                            Spacing="8">
                            <TextBlock
                                Text="Check for updates"
                                Style="{StaticResource BodyStrongTextBlockStyle}" />
                            <Border
                                x:Name="UpdateAvailabilityBadge"
                                Padding="10,3"
                                Background="{ThemeResource AccentFillColorDefaultBrush}"
                                CornerRadius="999"
                                Visibility="Collapsed"
                                VerticalAlignment="Center">
                                <TextBlock
                                    Text="Update available"
                                    Style="{StaticResource CaptionTextBlockStyle}"
                                    Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}" />
                            </Border>
                        </StackPanel>
                        <ProgressRing
                            x:Name="UpdateProgressRing"
                            Grid.Column="1"
                            Width="20"
                            Height="20"
                            IsActive="False"
                            Visibility="Collapsed" />
                    </Grid>

                    <ToggleSwitch
                        x:Name="UpdateCheckToggle"
                        Header="Automatically check on startup"
                        Toggled="UpdateCheckToggle_Toggled" />
                    <ComboBox
                        x:Name="UpdateChannelComboBox"
                        Header="Update channel"
                        Width="200"
                        HorizontalAlignment="Left"
                        SelectionChanged="UpdateChannelComboBox_SelectionChanged">
                        <ComboBoxItem Content="Stable" />
                        <ComboBoxItem Content="Pre-release" />
                    </ComboBox>
                    <TextBlock
                        Text="Stable checks published releases only. Pre-release also includes prerelease tags on GitHub."
                        Style="{StaticResource CaptionTextBlockStyle}"
                        Opacity="0.7"
                        TextWrapping="WrapWholeWords" />

                    <Grid HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            Grid.Column="0"
                            Text="Current version"
                            Style="{StaticResource BodyTextBlockStyle}"
                            VerticalAlignment="Center" />
                        <TextBlock
                            Grid.Column="1"
                            x:Name="CurrentVersionTextBlock"
                            Style="{StaticResource BodyTextBlockStyle}"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Opacity="0.8" />
                    </Grid>

                    <Grid HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            Grid.Column="0"
                            Text="Status"
                            Style="{StaticResource BodyTextBlockStyle}"
                            VerticalAlignment="Center" />
                        <TextBlock
                            Grid.Column="1"
                            x:Name="UpdateStatusTextBlock"
                            Style="{StaticResource BodyTextBlockStyle}"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Opacity="0.8"
                            TextWrapping="WrapWholeWords" />
                    </Grid>

                    <Grid
                        x:Name="LatestVersionRow"
                        HorizontalAlignment="Stretch"
                        Visibility="Collapsed">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            Grid.Column="0"
                            Text="Latest version"
                            Style="{StaticResource BodyTextBlockStyle}"
                            VerticalAlignment="Center" />
                        <TextBlock
                            Grid.Column="1"
                            x:Name="LatestVersionTextBlock"
                            Style="{StaticResource BodyTextBlockStyle}"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Opacity="0.8" />
                    </Grid>

                    <TextBlock
                        x:Name="UpdateErrorTextBlock"
                        Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                        TextWrapping="WrapWholeWords"
                        Visibility="Collapsed" />

                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Button
                            Grid.Column="0"
                            Grid.ColumnSpan="2"
                            x:Name="CheckNowButton"
                            Click="CheckNowButton_Click"
                            HorizontalAlignment="Stretch"
                            Content="Check now" />
                        <Button
                            Grid.Column="1"
                            x:Name="DownloadAndInstallButton"
                            Click="DownloadAndInstallButton_Click"
                            HorizontalAlignment="Stretch"
                            Content="Download and install"
                            Visibility="Collapsed" />
                    </Grid>
                </StackPanel>
            </Border>

            <!-- About Card -->
            <Border
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8"
                Padding="12">
                <StackPanel Spacing="12">
                    <!-- Title -->
                    <TextBlock
                        Text="About"
                        Style="{StaticResource BodyStrongTextBlockStyle}" />

                    <!-- GitHub Repository Row -->
                    <Grid HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            Grid.Column="0"
                            Text="GitHub Repository"
                            Style="{StaticResource BodyTextBlockStyle}"
                            VerticalAlignment="Center"/>
                        <Button
                            Grid.Column="1"
                            x:Name="GoToRepositoryButton"
                            Click="GoToRepositoryButton_Click"
                            HorizontalAlignment="Right">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE8A7;" FontSize="14"/>
                                <TextBlock Text="Go to repository"/>
                            </StackPanel>
                        </Button>
                    </Grid>

                    <!-- Owner Row -->
                    <Grid HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            Grid.Column="0"
                            Text="Owner"
                            Style="{StaticResource BodyTextBlockStyle}"
                            VerticalAlignment="Center"/>
                        <TextBlock
                            Grid.Column="1"
                            Text="Daniel D'Angeli"
                            Style="{StaticResource BodyTextBlockStyle}"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Opacity="0.8"/>
                    </Grid>

                    <!-- License Row -->
                    <Grid HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            Grid.Column="0"
                            Text="License"
                            Style="{StaticResource BodyTextBlockStyle}"
                            VerticalAlignment="Center"/>
                        <TextBlock
                            Grid.Column="1"
                            Text="MIT"
                            Style="{StaticResource BodyTextBlockStyle}"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Opacity="0.8"/>
                    </Grid>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>

</Page>
